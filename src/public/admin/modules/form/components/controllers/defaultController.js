(function (angular, undefined) {
    "use strict";

    angular.module("form").controller(
        "form.defaultController",
        [
            "$scope", "$state", "$window",
            function ($scope, $state, $window) {
                //Code
            }
        ]
    );
})(angular);
